buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath "com.android.tools.build:gradle:8.2.0"
    }
}
repositories {
    google()
    mavenCentral()
}
apply plugin: "com.android.application"
android { namespace "com.my.newproject"; compileSdk 34; defaultConfig { applicationId "com.my.newproject"; minSdk 21; targetSdk 34; versionCode 1; versionName "1.0" } }
dependencies { implementation "io.socket:socket.io-client:2.0.1" }
